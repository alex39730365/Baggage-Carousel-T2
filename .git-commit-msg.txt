Fix broken upstream endpoint and add retry/stale fallback to worker

The deployed worker was calling a nonexistent endpoint
(getBaggageArrivals), which the data.go.kr portal rejects immediately
with NO_OPENAPI_SERVICE_ERROR. Switch to getFltArrivalsBaggageClaimDesk,
the endpoint already used by api/baggage-arrivals.ts.

Also harden the proxy against the upstream's genuine slowness:
- Per-attempt timeout via AbortController instead of an unbounded fetch
- Retry once on failure/non-2xx before giving up
- Cache the last successful response (Cache API) and serve it as a
  stale fallback if every attempt fails, instead of returning 502

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
