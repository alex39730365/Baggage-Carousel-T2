# 수하물 캐러셀 현황 웹 — 통합 문서

> **본 문서**는 프로젝트 개요, 요구사항, 구축 이력, 아키텍처, 공공 API, 캐시·운영, 화면 기능, 구현 요약, 코드 색인을 **한 파일**에 모았습니다.  
> 연동·환경 변수 세부는 **`INTEGRATION.md`**, 변수 이름 목록은 **`.env.example`** 을 참고하세요.

---

## 1. 한 페이지 요약

| 항목 | 내용 |
|------|------|
| **제품** | 인천공항 **도착편 수하물 캐러셀 배정·시각**을 보는 **내부 참고용** 단일 페이지 대시보드 |
| **데이터** | 공공데이터포털 **「인천국제공항공사_항공기 수하물 정보(조업사용)」** — API 경로 기본값 `getFltArrivalsBaggageClaimDesk` (사내 스냅샷 URL 등으로 대체 가능, `INTEGRATION.md` 참고) |
| **사용자** | 현장·사무 등 수하물·운항 관련 업무 참고 |
| **기술** | React 18, TypeScript, Vite, Tailwind / **Vercel Serverless** `api/baggage-arrivals.ts` |
| **데이터 경로** | 브라우저는 **`GET /api/baggage-arrivals`만** 호출. 공공 API 직접 다수 호출은 하지 않음(서버에서 스냅샷·캐시). |

---

## 2. 요구사항·운영 반영 (비즈니스 관점)

**기획·품질 기준**

- 엑셀 **「Baggage Carousel 현황표」**와 유사: **시간 축 × 캐러셀 1~19번** 격자.
- 데이터는 **비교적 최신**이되, **공공 API 일일 호출 한도**를 넘기지 않도록 **서버 약 60초 캐시·동시 요청 합류** 등으로 설계.
- API **필드명·응답 형태 변동**에 대비한 정규화·복수 키 탐색.
- **모바일(카드형)·데스크톱(격자)**, 날짜·터미널·편명 검색·즐겨찾기(★), **수하물 처리 시간** 모드.
- **고정 스케줄**(`src/data/fixedSchedule.json`)을 API와 병합해 현장 보조 일정 반영.

**소스에 명시된 운영 요청(예시)**

| 요청 | 반영 |
|------|------|
| 웹 시작 시 **코드셰어 숨김 필터를 항상 켠 상태** | `BaggageCarouselBoard.tsx` — 로드 시 필터 기본 ON |
| 현장에서 보기 편한 편명만 | 2자+4자리 코드셰어 형태는 기본 숨김, **`KE` + 4자리 중 천의 자리가 2·8·9(차터)**인 편만 예외 표시 |

**추가 요청·회의 추적(선택)** — 아래 표에 메모해 두면 이후 인수인계에 유리합니다.

| 받은 날짜 | 요청 요약 | 반영 일자·파일(메모) |
|-----------|-----------|---------------------|
| | | |

---

## 3. 구축 일정 요약 (타임라인)

Git 커밋·변경 파일 기준 요약입니다.

| 날짜 | 한 줄 요약 |
|------|------------|
| **2026-04-17** | React/Vite/서버리스/API/보드 **초기 골격(MVP)** |
| **2026-04-18** | **약 1분 캐시**, **고정 스케줄**, 격자·데이터 로직 **대폭 확장** + 개발 문서 초안 |
| **2026-04-21** | `baggageApi` 등 **소규모 안정화** |
| **2026-04-22 ~ 23** | 보드·App **UI·연동 정리** |
| **2026-04-24** | 서버 API 강화, **`CarouselDataGrid`**, 분석용 JSON·스크립트 — **처리 시간·대량 데이터** 기반 |
| **2026-04-25** | 운영·캐시 설명을 별도 문서로 분리하던 시기(현재는 본 통합 문서에 포함) |
| **2026-05-03** | 전역 스타일·UI·API **넓은 범위 개선** |
| **2026-05-04** | **`INTEGRATION.md`**, 배포·타입·환경 변수 정합 |
| **2026-05-08** | 보드 **기능·UX 보완** |

---

## 4. 시스템 구성 (아키텍처)

```text
[브라우저]
    │  GET /api/baggage-arrivals (프론트는 약 60초마다 재조회)
    ▼
[Vercel Serverless: api/baggage-arrivals.ts]
    │  메모리 TTL 캐시 · 동시 요청 in-flight 합류
    │  (선택) Vercel KV — 인스턴스 간 스냅샷·STALE 폴백 등
    ▼
[apis.data.go.kr — getFltArrivalsBaggageClaimDesk]
    또는 사내 설정: BAGGAGE_UPSTREAM_SNAPSHOT_URL 등 (INTEGRATION.md)
```

**의도**: 사용자 PC가 공공 API를 반복 호출하지 않고, **서버 한 곳**에서 모아 **호출 한도·부하**를 관리합니다.

---

## 5. 공공데이터 API (핵심만)

| 항목 | 내용 |
|------|------|
| 데이터셋(포털명) | 인천국제공항공사_항공기 수하물 정보(조업사용) |
| 기본 URL | `https://apis.data.go.kr/B551177/statusOfBaggageClaimDesk/getFltArrivalsBaggageClaimDesk` |
| 인증 | 쿼리 `serviceKey` — **운영에서는 서버 환경 변수** (`DATA_GO_KR_SERVICE_KEY` 등). 로컬은 Vite 프록시 + `VITE_DATA_GO_KR_SERVICE_KEY` (`.env.example` 참고) |
| 주요 쿼리 | `type=json`, `numOfRows=1000`, `pageNo`, `searchDay`(0·1·2 병렬 수집 후 스냅샷 합침), 페이지 상한 등으로 비정상 대량 호출 방지 |
| 응답 사용 | `response.body.items` / `item` 배열 → 편명·시각·캐러셀 등 **정규화** (`src/lib/baggageApi.ts`). 필드명 변형은 복수 후보 키 순회로 대응 |

---

## 6. 캐시·운영

| 항목 | 내용 |
|------|------|
| 서버 TTL | 약 **60초** (인메모리) |
| 동시성 | 동일 시점 다중 요청 → **한 번의 upstream 수집에 합류** |
| HTTP 캐시 | 예: `Cache-Control: public, s-maxage=60, stale-while-revalidate=30` |
| 디버그 | 응답 헤더 `X-Baggage-Cache: HIT \| MISS \| JOIN` |
| 포털 한도(참고) | 활용 신청 기준 **일일 트래픽 한도**(문서상 예: 50,000건) — 실제 upstream 호출은 캐시·합류로 완화 |
| Vercel 점검 | `/api/baggage-arrivals` — 429·503·500, 캐시 비중, 응답 시간(p75/p95) |

**환경 변수**: `VITE_DATA_GO_KR_SERVICE_KEY`, `DATA_GO_KR_SERVICE_KEY` 외 사내 연동·CORS·스냅샷 URL 등은 **`INTEGRATION.md`·`.env.example`** 에 정리되어 있습니다. **실제 키는 저장소에 넣지 말고** 배포 환경 변수로만 관리하는 것을 권장합니다.

---

## 7. 화면·기능 요약

| 영역 | 내용 |
|------|------|
| 모드 | 모바일 카드 / **캐러셀 현황**(시간×캐러셀 격자) / **수하물 처리 시간** 격자 |
| 필터 | 기준 날짜, 터미널(전체·1·2), 코드셰어 필터(위 2절 규칙) |
| 검색·즐겨찾기 | 편명 검색·스크롤·강조, ★는 localStorage |
| 처리 시간 | `bagFirstTime` ~ `bagLastTime` 기반 분 단위 등 |
| 기타 | 핀치 줌, 10/11번 사이 가이드 라인, LIVE 갱신 시각, 비공식 참고 안내 |

---

## 8. 주요 구현·역할 (개발 관점 요약)

- **서버리스**: 공공 API 페이지네이션 수집 → 단일 JSON 스냅샷, 캐시·KV(선택), `vercel.json` 타임아웃 등.
- **프론트 데이터**: 서울 시간대, 날짜 버킷(예정 시각 우선), 캐러셀 번호 파싱, 편명 정리, 중복·점수 기반 병합, 고정 스케줄 merge, localStorage 보조.
- **UI**: `BaggageCarouselBoard`, `CarouselDataGrid` 중심.
- **배포**: Vercel + GitHub, `@vercel/analytics` 선택.

---

## 9. 로컬 실행·빌드

```bash
cd <프로젝트 루트>
npm ci          # 또는 npm install
# Windows: copy .env.example .env 후 키 입력
npm run dev
```

- 개발 서버: `http://localhost:5173` (포트는 `vite.config.ts` 기준)  
- 빌드: `npm run build` → `npm run preview`

---

## 10. 코드·문서 색인

| 구분 | 경로 |
|------|------|
| 프로젝트 설명 | `통합문서.md` (본 문서) |
| 진입 | `src/main.tsx`, `src/App.tsx` |
| UI | `src/components/BaggageCarouselBoard.tsx`, `CarouselDataGrid.tsx` |
| 데이터 | `src/hooks/useBaggageData.ts`, `src/lib/baggageApi.ts`, `src/types.ts`, `src/data/fixedSchedule.json` |
| API | `api/baggage-arrivals.ts` |
| 배포·프록시 | `vercel.json`, `vite.config.ts` |
| 연동·환경 | `INTEGRATION.md`, `.env.example` |

---

## 11. 한계·면책

- 원천 데이터 지연·오류 가능 — **공식 SSOT가 아닌 참고용** 도구로 설계됨.  
- 외부 API 한도·가용성은 **운영 정책·키·게이트웨이**에 따름.

---

*기존 개발과정·운영·구축 일지 문서를 통합해 이 파일로 유지합니다.*
